Beliefs:
  notary(n).
  gatekeeper(gk).

Plans:
  [ B(gatekeeper(X)) ; send(X, request, register(me)) ]

PC-rules:
  message(X, inform, registered(Y)) <- true |
  {
    print("OK")
  }
  message(X, inform, refused(Y)) <- true |
  {
    print("ANGRY")
  }