Beliefs:
  notary(n).

Plans:
  @cardtable(enterAsGatekeeper())

PC-rules:
  message(X, request, _, _, join) <- true |
  {
    @cardtable(sit(X));
    send(X, inform, _, _, ack)
  }

PR-rules:
  @cardtable(sit(X)); REST <- true |
  {
    send(X, inform, _, _, refuse)
  }