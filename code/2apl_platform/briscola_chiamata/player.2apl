
BeliefUpdates:

Beliefs:
  notary(n).
  gatekeeper(gk).

Plans:
  B(gatekeeper(X)) ;
  send(X, request, join)

Goals:

PG-rules:

PC-rules:
  message(X, inform, _, _, ack) <- true |
  {
    
  }

PR-rules:
